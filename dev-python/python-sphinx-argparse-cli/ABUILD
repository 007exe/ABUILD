pkgname=python-sphinx-argparse-cli
pkgver=1.10.0
pkgbuild=2
arch=("auto")

shortdesc=("  Render CLI arguments (sub-commands friendly) defined by argparse module.")

source=("https://github.com/tox-dev/sphinx-argparse-cli/archive/${pkgver}.tar.gz")

tags=("libs dev-python")

build_deps=("python python-build python-installer python-setuptools-scm python-wheel python-toml
python-sphinx python-docutils-stubs")

adddep=("python-sphinx python-docutils-stubs")

build() {
  go_src_dir
  SETUPTOOLS_SCM_PRETEND_VERSION=${pkgver} python -m build -nwx
  python -m installer -d ${pkgdir} dist/*.whl
  install -Dm 644 LICENSE.txt ${pkgdir}/usr/share/licenses/${pkgname}/LICENSE
}

# NOTE: Удаляем исходники пакета после сборки
# FIXME: mkpkg не умеет сохранять скачанные файлы под новыми именами, что может приводить к
# ошибками когда в кеше имеется архив с исходниками от другого пакета
after_build(){
  go_src_dir
  rm ${srcache}/${pkgver}.tar.gz
}
