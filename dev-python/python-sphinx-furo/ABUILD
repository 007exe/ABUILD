pkgname=python-sphinx-furo
pkgver=2022.12.07
pkgbuild=1
arch=("auto")

shortdesc=("A clean customizable documentation theme for Sphinx.")

source=("https://github.com/pradyunsg/furo/archive/${pkgver}.tar.gz")

tags=("libs dev-python")

adddep=("python-sphinx-basic-ng python-pygments python-beautifulsoup4")

# nodejs-lts-gallium
build_deps=("${adddep} python python-build python-installer npm
python-sphinx-theme-builder python-flit-core")

build() {
  go_src_dir
#  STB_USE_SYSTEM_NODE=true
  python -m build -nw
  python -m installer --destdir=${pkgdir} dist/*.whl
}

# NOTE: Удаляем исходники пакета после сборки
# FIXME: mkpkg не умеет сохранять скачанные файлы под новыми именами, что может приводить к
# ошибкам когда в кеше имеется архив с исходниками от другого пакета
after_build(){
  go_src_dir
  rm ${srcache}/${pkgver}.tar.gz
}
