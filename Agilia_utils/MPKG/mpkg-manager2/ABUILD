# ABUILD generated by spkg2abuild converter from mpkg-manager2-0.14.1_svnREVISION-1.spkg

# Package metadata
pkgname=mpkg-manager2
pkgver=0.16
pkgbuild=1
arch="auto"

# Package description
shortdesc=("MPKG package system: GUI front-end")
longdesc=("MPKG package system: GUI front-end")

# Source URL
source=("git:git://github.com/007exe/mpkg.git")

# Tags
tags="xapps x11-apps"

build_deps=" "

# Build system
BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr"

build() {
        go_src_dir
        burn_patches
        mkdir build
        cd build
        cmake $BUILD_KEYS ../manager2
        make -j${numjobs}
        make install DESTDIR=${pkgdir}
}

