# ABUILD generated by spkg2abuild converter from mpkg-gui-installer-0.14.1_svnREVISION-1.spkg

# Package metadata
pkgname=mpkg-gui-installer
pkgver=0.15
pkgbuild=1
arch="auto"

# Package description
shortdesc=("MPKG package system: GUI integration")
longdesc=("GUI integration tools for mpkg")

# Source URL
source=("git:git://github.com/007exe/mpkg.git")

# Tags
tags="xapps sys-pkgtools"

build_deps=" "

# Build system
BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr -DLIB_SUFFIX=$LIBDIRSUFFIX"

build() {
        go_src_dir
        burn_patches
        mkdir build
        cd build
        cmake $BUILD_KEYS ../mpkg-gui-installer
        make -j${numjobs}
        make install DESTDIR=${pkgdir}
}
