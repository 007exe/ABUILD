# ABUILD generated by spkg2abuild converter from mpkg-packagebuilder-0.14.1_svnREVISION-1.spkg

# Package metadata
pkgname=mpkg-packagebuilder
pkgver=0.16
pkgbuild=1
arch="auto"

# Package description
shortdesc=("MPKG package system: utility for creating and editing packages")
longdesc=("Graphic utility for creating and editing packages, both binary and SPKG.")

# Source URL
source=("git:git://github.com/007exe/mpkg.git")

# Tags
tags="xapps x11-apps"

build_deps=" "

# Manually-added dependencies
adddep="mkpkg"

# Build system
BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr"

build() {
        go_src_dir
        burn_patches
        mkdir build
        cd build
        cmake $BUILD_KEYS ../packagebuilder
        make -j${numjobs}
        make install DESTDIR=${pkgdir}
}
