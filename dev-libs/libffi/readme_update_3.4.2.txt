Update or rebuild packages (glib2, gobject-introspection, gtk3, guile2, llvm-libs, p11-kit, python, python-gobject, wayland, xorg-server) then remove libffi.so.7 from the previous version and remove the --disable-exec-static-tramp parameter 

Обновить или пересобрать пакеты (glib2, gobject-introspection, gtk3, guile2, llvm-libs, p11-kit, python, python-gobject, wayland, xorg-server) после чего удалить libffi.so.7 от пред идущей версии и убрать параметр --disable-exec-static-tramp
