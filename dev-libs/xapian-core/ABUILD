pkgname=xapian-core
pkgver=1.4.23
pkgbuild=1
arch=("auto")

shortdesc=("Open source search engine library.")

source=("https://oligarchy.co.uk/xapian/${pkgver}/${pkgname}-${pkgver}.tar.xz")

tags=("dev-libs libs")

adddep=("bash gcc-libs glibc zlib")

build_deps=("${adddep} util-linux")

build() {
  go_src_dir
  burn_patches
  autoreconf -vfi
  CXXFLAGS=${CXXFLAGS/-Wp,-D_GLIBCXX_ASSERTIONS}
  ./configure \
    --prefix=/usr \
    --libdir=/usr/lib \
    --disable-static
  make -j${numjobs}
  make DESTDIR=${pkgdir} install
  install -vDm 644 {AUTHORS,ChangeLog,HACKING,NEWS,README} -t ${pkgdir}/usr/share/doc/${pkgname}
}
