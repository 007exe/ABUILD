# ABUILD generated by mkpkg_generator.sh

pkgname=neofetch
pkgver=7.1.0
pkgbuild=1
arch=("auto")

shortdesc=("A CLI system information tool written in BASH that supports displaying images.")

tags=("app-shells console")

source=("https://github.com/dylanaraps/neofetch/archive/$pkgver.tar.gz")

build_deps="mkpkg git"

adddep="bash"

backup=('etc/neofetch/config.conf')

build() {
  go_src_dir
  make DESTDIR="$pkgdir" install
  install -Dm644 LICENSE.md "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE.md"
}
