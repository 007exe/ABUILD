pkgname=layer-shell-qt
pkgver=5.26.0
pkgbuild=1
arch=("auto")

shortdesc=("Qt component to allow applications make use of Wayland wl-layer-shell protocol.")
longdesc=("Qt component to allow applications make use of Wayland wl-layer-shell protocol.")

source=("https://mirror.yandex.ru/mirrors/ftp.kde.org/stable/plasma/${pkgver}/${pkgname}-${pkgver}.tar.xz")

tags=("plasma kde-plasma")

build_deps=("qt5-wayland extra-cmake-modules wayland-protocols")

build() {
  go_src_dir
  burn_patches
  cmake -B build \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_INSTALL_LIBDIR=lib \
    -DBUILD_TESTING=OFF
  cmake --build build -j${numjobs}
  DESTDIR=${pkgdir} cmake --install build
}
