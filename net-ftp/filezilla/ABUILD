pkgname=filezilla
pkgver=3.62.2
pkgbuild=1
arch=("auto")

shortdesc=("Fast and reliable FTP, FTPS and SFTP client.")

source=("https://download.filezilla-project.org/client/FileZilla_${pkgver}_src.tar.bz2")

tags=("network net-ftp")

build_deps=("dbus xdg-utils wxwidgets-gtk3 libidn hicolor-icon-theme sqlite gnutls libfilezilla")

adddep=("dbus xdg-utils wxwidgets-gtk3 libidn hicolor-icon-theme sqlite gnutls libfilezilla")

build() {
  go_src_dir
  burn_patches
  autoreconf -vif
  ./configure \
    --prefix=/usr \
    --libdir=/usr/lib \
    --disable-manualupdatecheck \
    --disable-autoupdatecheck \
    --with-pugixml=builtin
  make -j${numjobs}
  make DESTDIR=${pkgdir} install
}
