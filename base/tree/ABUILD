# ABUILD generated by mkpkg_generator.sh

pkgname=tree
pkgver=1.8.0
pkgbuild=1
arch=("auto")

shortdesc=("tree (a program to display a directory tree)")
longdesc=("Tree is a recursive directory listing program that produces a depth indented listing of files, which is colorized ala dircolors if the LS_COLORS environment variable is set and output is to tty. With no arguments, tree lists the files in the current directory. The tree utility was written by Steve Baker.")
tags=("base app-text")

source=("http://deb.debian.org/debian/pool/main/t/tree/${pkgname}_${pkgver}.orig.tar.gz")
build_deps="glibc"

build() {
  go_src_dir
  cd "${srcdir}/${pkgname}-${pkgver}"
  make
  make prefix="${pkgdir}/usr" \
    MANDIR="${pkgdir}/usr/share/man/man1" \
    LIBDIR="/usr/lib$LIBDIRSUFFIX" \
    install
}
