# ABUILD generated by mkpkg_generator.sh

pkgname=xfsprogs
pkgver=5.8.0
pkgbuild=1
arch=("auto")

shortdesc=("Tools to use the XFS filesystem")
tags=("base sys-fs")

source=("https://mirrors.edge.kernel.org/pub/linux/utils/fs/xfs/xfsprogs/xfsprogs-${pkgver}.tar.gz")
build_deps="util-linux-ng"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib${LIBDIRSUFFIX} \
	--sbindir=/usr/bin \
	--bindir=/usr/bin \
	--datadir=/usr/share \
	--mandir=/usr/share/man \
	--enable-shared=yes \
	--enable-gettext=yes"

after_build() {
	make DIST_ROOT="${pkgdir}" PKG_ROOT_SBIN_DIR="/usr/bin" install install-dev
}
