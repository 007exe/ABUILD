# ABUILD generated by mkpkg_generator.sh

pkgname=findutils
pkgver=4.7.0
pkgbuild=1
arch=("auto")

shortdesc=("Utilities to locate files")
longdesc=("This package contains the GNU find and xargs programs. The find and xargs implementations comply with POSIX 1003.2. They also support some additional options, some borrowed from Unix and some unique to GNU.")

tags=("base sys-base")
# Новая ссылка
#source=("ftp://ftp.gnu.org/pub/gnu/findutils/${pkgname}-${pkgver}.tar.gz")
# Старая ссылка до версии 4.6
source=("https://alpha.gnu.org/gnu/findutils/${pkgname}-${pkgver}.tar.gz")
build_deps="glibc"
adddep="bash"

before_build() {
	go_src_dir
	sed -i '/^SUBDIRS/s/locate//' Makefile.in
}

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/share/man --docdir=/usr/share/doc --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

