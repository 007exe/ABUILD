# ABUILD generated by mkpkg_generator.sh

pkgname=glibc-i18n
pkgver=2.20
pkgbuild=2
arch=("noarch")

shortdesc=("glibc (GNU C libraries) - virtual package")
longdesc=("Virtual package glibc-i18n SHOULD BE REMOVED !!!!.")

tags=("virtual sys-libs")

build_deps=" "

build() {
	echo "Building virtual package"
}
