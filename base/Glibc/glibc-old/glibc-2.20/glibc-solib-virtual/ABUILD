# ABUILD generated by mkpkg_generator.sh

pkgname=glibc-solibs
pkgver=2.20
pkgbuild=2
arch=("noarch")

shortdesc=("glibc (GNU C libraries) - virtual package")
longdesc=("Virtual package glibc-solibs SHOULD BE REMOVED !!!!.")

tags=("virtual sys-libs")

build_deps=" "

build() {
	echo "Building virtual package"
}
