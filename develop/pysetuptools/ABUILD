# ABUILD generated by mkpkg_generator.sh

pkgname=pysetuptools
pkgver=3.0
pkgbuild=1
arch=("noarch")

shortdesc=("Easily build and distribute Python packages (Virtual package.)")

tags=("dev-python virtual")

adddep="python-setuptools"

build_deps="bash"

build(){
  echo "End build"
}
