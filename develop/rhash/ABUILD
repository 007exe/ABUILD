# ABUILD generated by mkpkg_generator.sh

pkgname=rhash
pkgver=1.4.0
pkgbuild=1
arch=("auto")

shortdesc=("Utility for verifying hash sums of files")

tags=("libs dev-libs")

source=("https://github.com/rhash/RHash/archive/v$pkgver.tar.gz")

build_deps="glibc"

config_files="etc/rhashrc"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --extra-cflags="$CFLAGS" --extra-ldflags="$LDFLAGS" --mandir=/usr/share/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

