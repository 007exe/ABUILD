# ABUILD generated by mkpkg_generator.sh

pkgname=yandex-disk
pkgver=0.1.5.1039
pkgbuild=1
arch=("auto")

shortdesc=("Yandex.Disk keeps your files with you at all times.")

tags=("xapps net-ftp")

source=("https://repo.yandex.ru/yandex-disk/deb/pool/main/y/yandex-disk/yandex-disk_${pkgver}_amd64.deb")

build_deps="glibc gcc-libs zlib libarchive"

adddep="glibc"

build() {
	go_src_dir
cp $srcache/yandex-disk_${pkgver}_amd64.deb .
	ar x *.deb
	bsdtar xf data.tar.gz -C $pkgdir
	install -Dm0755 ${filedir}/yandex-disk.initd ${pkgdir}/etc/init.d/yandex-disk
	install -Dm644 "${filedir}/yandex-disk.service" "${pkgdir}/usr/lib/systemd/user/yandex-disk.service"
}
